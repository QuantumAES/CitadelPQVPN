# CitadelPQVPN — программа системной проверки по CWE и проактивные меры

**Дата:** 2026-08-14 · **Ревизия:** `9431cfe` (дерево чистое) · **Оптика:** DevSecOps-процесс, а не
разовый аудит.

Четыре аудита (2026-07…08) искали **конкретные дефекты** и нашли их: 4 HIGH, 11 MEDIUM, 15 LOW,
все закрыты кроме осознанно оставленных (`docs/SECURITY-AUDIT-4-2026-08.md §20`). Этот документ про
другое — про **покрытие классов слабостей**: чтобы про каждый класс можно было сказать «проверяется
вот так, гейт вот такой, последний прогон тогда-то», а не «в тот раз не всплыло».

Аудит отвечает на вопрос «что сломано сейчас». Программа ниже отвечает на «что мы делаем, чтобы
следующий класс не проехал молча».

---

## 1. Метод и легенда

За основу взяты **CWE Top-25 (2024)** и профильные для продукта классы: привилегированные демоны,
разбор недоверенных пакетов, криптографические протоколы, мобильная и оконная платформы, цепочка
поставки. Каждый класс проходит четыре вопроса:

1. **Есть ли поверхность** — где именно у нас этот класс может проявиться (файл, граница доверия);
2. **Какой контроль стоит** — что уже сделано (со ссылкой на код);
3. **Чем проверяется** — тест, гейт CI, живой прогон, ручная процедура;
4. **Чего не хватает** — конкретная работа с фазой из §5.

Статусы:

* ✅ — контроль есть **и** есть автоматический гейт (падает при регрессии);
* 🟡 — контроль есть, но держится на дисциплине/ревью, автоматической проверки нет;
* ⬜ — класс релевантен, систематически не проверялся.

Статус ставится по **классу**, а не по конкретной находке: «✅» не значит «дефектов нет», значит
«регрессия этого класса будет поймана гейтом».

---

## 2. Карта поверхностей (границы доверия)

Всё, что ниже, — вход из-под контроля противника хотя бы в одном сценарии. Порядок — по «дешевизне
доступа» для атакующего.

| # | Граница | Что пересекает | Где в коде | Кто может подать |
|---|---|---|---|---|
| S1 | **Провод L1 (obfs)** | UDP/TCP-байты до всякой аутентификации | `crates/citadel-obfs/src/lib.rs:296` (`Opener::open`), `crates/citadel-quic/src/obfs_socket.rs` | кто угодно в сети |
| S2 | **Провод L2 (QUIC/TLS)** | хендшейк, до проверки pin/подписи | `crates/citadel-quic/src/client.rs`, `bin/citadel-m1.rs` | кто угодно, кто знает адрес и PSK эпохи |
| S3 | **Капсулы и датаграммы туннеля** | `ADDRESS_ASSIGN`/`REQUEST`, inner-IP, keepalive, подсказка idle | `crates/citadel-masque/src/lib.rs:136,180,213,305` | аутентифицированный пир (в т.ч. **скомпрометированный exit**) |
| S4 | **Канал издателя** | кадры Layer-1, VOPRF, кадр эпохи, гейт/активация | `crates/citadel-token/src/lib.rs:157,554`, `pqid.rs:256` | любой, кто дотянулся до token-порта |
| S5 | **Admin-канал** | CBOR-команды реестра | `crates/citadel-token/src/admin.rs:383,439` | абонент из туннеля (до admin-подписи) |
| S6 | **`citadel://` и bundle** | ссылка из QR/буфера/файла | `crates/citadel-client/src/creds.rs:347` | тот, кто дал ссылку человеку |
| S7 | **Файл хранилища** | заголовок, таблица слотов, шифртекст | `crates/citadel-client/src/vault.rs:257` | бэкап, синхронизация, сосед по устройству |
| S8 | **Файлы состояния сервера** | реестр, `spent.bin`, `ports.env`, ключи | `crates/citadel-token/src/admin.rs:145`, `main.rs:440+` | root на боксе (A-2), ошибки прав |
| S9 | **Граница привилегий Linux** | строки конфигурации от неприв. приложения | `crates/citadel-vpnd/src/valid.rs:47,89,167`, `proto.rs:210`, `citadel-helper` | локальный пользователь |
| S10 | **Граница привилегий Windows** | кадры named pipe к службе SYSTEM | `crates/citadel-winnet/src/lib.rs:86,204`, `crates/citadel-winsvc/src/main.rs:404-740` | любой интерактивный пользователь |
| S11 | **FFI** | Rust ↔ Dart ↔ Kotlin/JNI, включая мастер-ключ хранилища | `app/rust/src/api/citadel.rs`, `app/rust/src/android_jni.rs`, `app/lib/biometric.dart` | другой процесс на устройстве (после компрометации) |
| S12 | **Деплой** | установщик, docker, релизные артефакты | `tools/install-citadel-server.sh`, `.github/workflows/*` | сеть при установке, цепочка поставки |

---

## 3. CWE-матрица

### 3.1. Память и `unsafe` (CWE-787, CWE-125, CWE-416, CWE-476, CWE-190/191)

| CWE | Поверхность у нас | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 787/125 запись/чтение за границей | Safe Rust почти везде; реальный риск — 47 `unsafe` в `citadel-winsvc` (WFP/Win32-структуры, union-поля), 68 в `app/rust` (JNI), `citadel-tun`/`citadel-vpnd` (ioctl, sendfd) | границы буферов проверяются до крипты (`vault.rs:257`, `obfs`), у каждого `unsafe` — комментарий `SAFETY` | 🟡 | **Ф2:** MIRI на тестах крейтов без FFI, ASAN-прогон `citadel-tun`/`citadel-vpnd`, `cargo geiger` в отчёт CI |
| 416 use-after-free | `Arc`/`Weak` в pacer'е (`obfs_socket.rs:756` держит `Weak`, чтобы задача умирала вместе с сокетом), хэндлы WFP/Keystore | владение выражено типами; `Drop`-порядок задокументирован (`winsvc/main.rs:991`) | 🟡 | **Ф2:** те же прогоны + отдельный тест «задача pacer'а умирает вместе с endpoint'ом» (есть косвенный) |
| 190/191 переполнение | длины из пакетов и заголовков: `pad_len_*`, `slots_len`, varint, MTU-арифметика | `saturating_*`/`checked_*` в разборе (`vault.rs:274`, `obfs_socket.rs:624`) | 🟡 | **Ф0:** `overflow-checks = true` в релизном профиле серверных бинарей (сейчас профиля нет вовсе ⇒ в release переполнение молча заворачивается) |
| 476 разыменование null | только через FFI/Win32 | проверки `HANDLE`/указателей на месте | ✅ (компилятор + тесты) | — |

### 3.2. Разбор недоверенного ввода (CWE-20, CWE-502, CWE-130, CWE-1284, CWE-674)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 20 валидация ввода | S1–S10 целиком | `parse, don't validate` на границах привилегий (`vpnd/valid.rs`), клампы префикса и адреса на клиенте (`client.rs:653`), границы Argon2 и таблицы слотов (`vault.rs:109,277`), `EgressPolicy` перед F2 (`dataplane.rs:209+`) | 🟡 | **Ф1:** фаззинг всех разборщиков (перечень — Приложение A). Тесты покрывают известные случаи; фаззер ищет неизвестные |
| 502 десериализация | CBOR (`ciborium`) в бандле, admin-командах, кадрах vpnd, таблице слотов | формат без кода/типов (в отличие от pickle-подобных), потолки размера до разбора (`vault.rs:277`, `MAX_SLOTS`) | 🟡 | **Ф1:** цель фаззинга `decode_config` (S10 — пересекает границу привилегий) и `parse_registry`; **Ф3:** явный потолок вложенности |
| 130 несогласованная длина | varint + `len` в капсулах и кадрах | `decode` возвращает `Option`, длины сверяются с буфером (`masque:136`, `token/lib.rs:157`) | 🟡 | **Ф1** |
| 1284 неверная величина | `prefix`, `mtu`, `idle_hint`, `epoch_secs`, Argon2-параметры | клампы: `12..=30` для префикса, границы Argon2, `idle_hint` влияет только на выбор из двух констант (`dataplane.rs:306-326`) | ✅ (юнит-тесты на каждый кламп) | — |
| 674 неконтролируемая рекурсия | вложенный CBOR | потолок размера входа есть, потолка глубины нет | ⬜ | **Ф3:** тест «глубоко вложенный CBOR не роняет разбор» + при необходимости лимит |

### 3.3. Ресурсы и доступность (CWE-400, CWE-770, CWE-405)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 400/770 исчерпание | pre-auth соединения издателя, очередь пейсинга, память Argon2 из чужого файла, spent-журнал | гейт H-1 с потолками и вытеснением (`token/main.rs:225-268`), `SEND_QUEUE_CAP`, границы Argon2 (L-2), квоты A6 | ✅ (юнит + docker-харнес) | **R2.1/R2.2** из `SECURITY-ROADMAP §12`: квоты переживают рестарт, вытеснение по нагрузке адреса |
| 405 асимметричный расход | chaff (наш собственный трафик), rate-limit обоих направлений | бюджет chaff (`obfs_socket.rs:697`), `RateLimits{up,down}` | ✅ | — |
| — | «маскировка жжёт батарею» как класс | П1–П8 (`COVER-TRAFFIC-BATTERY-2026-08.md §7`) | 🟡 | замер на устройстве (`§6 п.3` там же) |

### 3.4. Аутентификация, авторизация, привилегии (CWE-287, 306, 862, 863, 269, 250, 522)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 287/306 | Layer-1 (гибрид Ed25519+ML-DSA), admin-кадр, keysync, активация ссылки | доменное разделение + EKM channel binding на каждом канале (`pqid.rs`, `admin.rs:383`); «не настроен id → канал закрыт» | ✅ (негативные тесты: чужой ключ, чужой EKM, кросс-доменный replay) | — |
| 862/863 | admin-операции, self-lockout, роль ссылки | проверка `admin_id` на сервере (UI-гейт вторичен), запрет revoke собственного `client_id` | ✅ (ТЕСТ 21 харнеса) | **A-2**: root на боксе подменяет `admin_id` — внешней проверяемости нет (§20.1 отчёта) |
| 269/250 | exit (NET_ADMIN), издатель, `citadel-helper`, служба Windows | `cap_drop: ALL` издателю, минимальный набор exit'у, `drop_privileges` после поднятия сети, служба линкует только `citadel-winnet` | ✅ (compose + лог + тесты плана) | живая проверка `capsh --print` (§11.1) |
| 522 защита кред | vault v4 (слоты, Argon2id, TEE), zeroize, ссылка с окном и одноразовостью | `vault.rs`, `BiometricVault.kt`, M-9 | 🟡 | **R1.2** автозамок (CWE-613 ниже), **R1.3** буфер обмена |

### 3.5. Криптография (CWE-327, 330/338, 323, 347, 295, 916)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 327 слабый алгоритм | KEX, подписи, AEAD, KDF | гибрид X25519MLKEM768, ML-DSA-65 + Ed25519, AES-256-GCM/ChaCha20-Poly1305, BLAKE3-derive | ✅ | **A-3**: Layer-2 (VOPRF) классический — граница зафиксирована в `SPEC §8` |
| 330/338 качество случайности | ключи, seed, nonce obfs, джиттер таймингов | ключи и seed — `aws_lc_rs::SystemRandom`; nonce chaff и джиттер — `rand::thread_rng` (ChaCha-CSPRNG) | 🟡 | **Ф3:** политика «крипто-случайность берётся ровно из одного источника» + тест-линт на `thread_rng` в крипто-путях (сейчас смешение безопасно, но это держится на знании) |
| 323 повторное использование nonce | obfs (per-session соль `sid` 16 Б + случайный nonce), AEAD хранилища | KDF-контексты `obfs/v2/*`, per-session ключ; новый nonce на каждую запись файла | ✅ (байт-векторы + тесты) | — |
| 347 проверка подписи | Layer-1, admin, keysync, активация, релизные артефакты | verify до любых действий; minisign на релизе (`install-citadel-server.sh:456`) | ✅ | **N-7**: docker ставится скриптом без проверки |
| 295 проверка сертификата | PQ-TLS к издателю, QUIC к exit'у | pin fail-closed, ML-DSA-доказательство владения ключом, insecure-эскейпы только за фичей `insecure-dev` | ✅ | — |
| 916 стоимость хеша пароля | мастер-пароль хранилища | Argon2id ~1–2 с (256 MiB/t=4 десктоп, 64 MiB/t=8 Android), параметры в файле + апгрейд при открытии | ✅ | — |

### 3.6. Гонки и время (CWE-362, CWE-367, CWE-613)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 362 гонка | захват слота сессии (Windows), эпохи и ключи, реестр | проверка владельца под тем же мьютексом, что и захват (`winsvc/main.rs`), атомарная запись реестра tmp+rename | ✅ | — |
| 367 TOCTOU | опознание клиента пайпа по PID → путь образа | текущий контроль — путь из install-dir + SDDL + mandatory label | 🟡 | **R3.2** (L-9): подпись образа + защита от PID-reuse |
| 613 срок сессии | **открытое хранилище живёт до закрытия приложения** | только ручной замок | ⬜ | **R1.2** автозамок — единственный пункт этого класса |

### 3.7. Утечка информации (CWE-200, CWE-359, CWE-532, CWE-209)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 200/359 приватные данные | no-logs на серверных ролях, адреса назначения, `client_id` | назначения только под `Citadel_DEBUG_LOG`, счётчики трафика не ведутся на exit'е (`dataplane.rs:347`), метки абонентов — только в vault админа | ✅ | **A-1**: издатель раз в эпоху видит `client_id`+IP (архитектура) |
| 532 секреты в логах | журнал клиента (ring), stderr сервера | секреты не печатаются, текст пира санируется (`peer_text`, L-15) | 🟡 | **Ф3:** авто-тест «дамп журнала не содержит ни одного известного секрета» (сгенерировать профиль с маркерными байтами и грепнуть) |
| 209 сообщения об ошибках | отказы издателя, отказы хранилища | коды вместо текста в гейте (`build_gate_frame`), «нет admin_id» неотличимо от «чужой ключ» | ✅ | — |

### 3.8. Конфигурация, дефолты, цепочка поставки (CWE-1188, 732/276, 798, 494, 1104)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 1188 небезопасный дефолт | **77 различных переменных `Citadel_*`** (подсчёт по дереву на `9431cfe`) плюс флаги установщика | fail-closed при мусорном значении (M-1/M-2/M-7 закрыты именно так), rate-limit включён установщиком, insecure-эскейпы вне релиза | 🟡 | **P-2/P-3:** реестр дефолтов + тест «мусор в переменной = отказ, а не отключение защиты» для КАЖДОЙ переменной |
| 732/276 права | ключи 0600, `spent.bin`, том издателя/exit'а, SDDL пайпа и службы | явные `chmod`/`set_file_perms_600`, разбор граблей с `cap_drop` и `CAP_CHOWN` (§14.3) | 🟡 | **Ф5:** тест-скрипт «после установки в `$DIR/keys` нет файла с правами шире 0600» |
| 798 зашитые креды | демо-стенд | `Citadel_DEMO_STAND` + `guard_weak_seed` роняет старт вне стенда (L-10) | ✅ | **Ф0:** сканер секретов (gitleaks) в CI — сейчас его нет |
| 494 загрузка без проверки | релизные бинари — minisign; **docker — `curl \| sh`** | `install-citadel-server.sh:456` против `:406` | 🟡 | **R2.3** (N-7) |
| 1104 неподдерживаемые компоненты | дерево зависимостей | `cargo-deny` в CI и локально; исключение только `RUSTSEC-2024-0436` (`paste` через `ratatui`, compile-time) | ✅ | **Ф5:** SBOM (CycloneDX) в артефактах релиза |

### 3.9. Инъекции (CWE-78/77/88, CWE-22)

| CWE | Поверхность | Контроль | Статус | Чего не хватает |
|---|---|---|---|---|
| 78/77/88 | 21 вызов `Command::new` (`ip`, `iptables`, `netsh`, `route`, docker) | argv-массивы без shell; строки от приложения проходят `parse, don't validate` (`vpnd/valid.rs`) до подстановки | ✅ (юнит-тесты негативов) | **Ф1:** фаззинг `valid::parse*` как формальное подтверждение |
| 22 обход пути | пути хранилища/пинов из окружения, имена профилей | имена саниризуются и ограничены (`vault.rs:314`), пути не склеиваются из недоверенных строк | 🟡 | **Ф1:** фаззинг `from_uri` + проверка, что имя профиля не участвует в путях |

---

## 4. Что из этого уже автоматизировано

Гейты, которые падают сами (`.github/workflows/ci.yml`): `cargo test --workspace` (**332 теста**),
`cargo clippy --workspace --all-targets -- -D warnings`, `cargo deny --workspace check`,
`flutter analyze`, кросс-чек `x86_64-pc-windows-gnu`, JNI-мост, docker-харнесы (`run-demo.sh`,
`run-cli-tests.sh`). Отдельный `windows.yml` держит скоуп `citadel-winnet/-winsvc/-client`.

Чего в гейтах **нет**: фаззинга, MIRI/санитайзеров, сканера секретов, SBOM, проверки прав файлов
после установки, проверки воспроизводимости сборки.

---

## 5. Роадмап проверки

Фазы независимы, кроме Ф0 (он даёт инфраструктуру). Каждая заканчивается **гейтом**, иначе она
превращается в разовую акцию, которую через месяц никто не повторит.

### Ф0 — «дешёвое сегодня» (эффорт S, 1 день)

| Шаг | Что | Критерий выхода |
|---|---|---|
| Ф0.1 | `gitleaks` (или `trufflehog`) отдельным job'ом CI по всей истории и на каждый PR | job зелёный, конфиг с исключениями для тестовых векторов |
| Ф0.2 | `[profile.release] overflow-checks = true` для серверных и движковых бинарей | сборка релиза проходит, бенчмарк (`docs/BENCHMARKS.md`) не проседает более чем на 3 % |
| Ф0.3 | `cargo geiger` отчётом (не гейтом) + артефакт в CI | видно, в каких крейтах `unsafe` растёт |
| Ф0.4 | Тест «права файлов после установки» в docker-харнесе | `find $DIR/keys -perm /077` пуст |

### Ф1 — фаззинг разборщиков (эффорт M, 2–3 дня + фон)

`cargo-fuzz` (libFuzzer) на разборщики из Приложения A. Правило: **цель = функция, которая читает
байты от противника и не имеет права паниковать**. Паника = падение (в Rust это не UB, но на
сервере это отказ соединения, а в некоторых местах — потока).

* Ф1.1 — цели S1/S3 (провод): `obfs::open`, `masque::capsule::decode`, `decode_assigned_v4`,
  `decode_idle_hint`, `ip::parse_ipv4`, `varint::decode`.
* Ф1.2 — цели S4/S5 (издатель): `read_frame` + `parse_client_frame`, `parse_gate_frame`,
  `parse_registry`, CBOR-команды admin.
* Ф1.3 — цели S6/S7 (клиент): `CredentialLink::from_uri`, `Vault::open` (заголовок + слоты).
* Ф1.4 — цели S9/S10 (границы привилегий, **высший приоритет**): `vpnd::valid::parse*`,
  `winnet::decode_config`, `winnet::parse_stream`.

**Гейт:** job `fuzz-smoke` в CI — каждая цель 60 с на корпусе из репозитория; найденные падения
фиксируются как обычные тесты в `tests/` (корпус живёт в `fuzz/corpus`, растёт от найденного).
**Критерий выхода:** 12 целей, ни одного падения на 10 минут каждой локально, smoke в CI.

### Ф2 — `unsafe` под инструментом (эффорт M)

* Ф2.1 — `cargo +nightly miri test` по крейтам без FFI/сети (`citadel-obfs`, `citadel-masque`,
  чистая логика `citadel-winnet`, `plan`-модули). Miri не пройдёт там, где есть syscalls — это
  ожидаемо, список исключений фиксируется явно.
* Ф2.2 — ASAN/LSAN-прогон тестов `citadel-tun`, `citadel-vpnd`, `citadel-helper`
  (`RUSTFLAGS=-Zsanitizer=address`, nightly).
* Ф2.3 — ревизия каждого `unsafe` в `citadel-winsvc` и `app/rust`: у блока должен быть
  `SAFETY`-комментарий, отвечающий на «почему это верно», а не «что делает».
* **Гейт:** Ф2.1 в CI (nightly job, allow-failure первый месяц); Ф2.3 — правило ревью.

### Ф3 — инварианты протокола как свойства (эффорт M)

Перевести в `proptest`/`quickcheck` то, что сейчас проверяется точечными примерами:

* anti-replay и окно `packet_id`; `pad_len_*` (никакая длина не выходит за MTU и не даёт нулевой
  padding); MTU-инвариант; `spent`-множество (токен не проходит дважды ни при какой перестановке
  событий); границы эпох (ключ под трафиком переживает смену, prev принимается ровно окно);
  гейт (потолки и вытеснение при любых последовательностях admit/release).
* Отдельно — **тест на отсутствие секретов в журнале** (§3.7): профиль с маркерными байтами,
  прогон сценария, grep по дампу.
* **Гейт:** обычный `cargo test`.

### Ф4 — платформы (эффорт M, требует устройств)

* **Android:** чеклист OWASP MASVS-STORAGE/CRYPTO/AUTH применительно к vault v4 и биометрии;
  проверка `allowBackup`/`exported`-компонентов манифеста; StrictMode в отладочной сборке;
  ручной прогон N-1 (фолбэк IPv6) и «новый отпечаток убивает ключ».
* **Windows:** ACL пайпа и службы (`sc sdshow`), проверка, что kill-switch рвёт установленные
  потоки (**после R3.1**), негативы опознания клиента (**после R3.2**), M-5 вживую.
* **Гейт:** ручной чеклист приёмки релиза (§6, P-8), результаты — в `docs/RELEASE.md`.

### Ф5 — цепочка поставки и деплой (эффорт M)

* SBOM (`cargo cyclonedx`) в артефакты релиза; SBOM APK — отдельно.
* Проверка воспроизводимости: две сборки одного тега на разных машинах → одинаковые sha256
  (или честная запись, чего именно не хватает для детерминизма).
* Docker без `curl | sh` (**R2.3**), минимизация образа, digest-пин базового образа.
* **Гейт:** `release.yml` публикует SBOM рядом с minisign-подписью; отсутствие SBOM = провал job'а.

### Ф6 — периодичность

| Что | Как часто | Кто/чем |
|---|---|---|
| Полный прогон гейтов | каждый PR | CI |
| `cargo deny` + обновление advisory-базы | еженедельно (расписание) | CI cron |
| Фаз-кампания (по 1 ч на цель) | ежемесячно | локально, корпус в репозиторий |
| Сверка документации с кодом (S3-трек) | каждый релиз | чеклист приёмки |
| Ре-скан по этой матрице | ежеквартально + после любого слома провода | ручной, результат — новая строка в §7 |

---

## 6. Проактивные меры (конструкция, а не проверка)

Проверки ловят ошибку после того, как она сделана. Ниже — то, что мешает её сделать.

### P-1. Инвариант «сокет мимо своего туннеля» — типом, а не дисциплиной

Инвариант `protect` ломался **дважды** (obfs-TCP 2026-08-06, QUIC/UDP 2026-08-12) и оба раза
выглядел как «плохая сеть»: хендшейк проходит, данные встают при подъёме TUN. Сейчас маршрут —
явный параметр (`citadel_protect::Route`), но создать сокет мимо него по-прежнему можно.
**Мера:** единственная фабрика исходящих сокетов движка, возвращающая тип, который умеет
`connect` только после решения о маршруте; прямые `UdpSocket::bind`/`TcpStream::connect` в
движковых крейтах запретить lint'ом (`clippy.toml` disallowed-methods).

### P-2. Реестр дефолтов, который нельзя молча поменять

> **Первый экземпляр меры уже есть** (заход 12): настройка автозамка хранилища читает мусор в
> файле как ДЕФОЛТ, а не как «выключено», и это зафиксировано тестом
> (`autolock_garbage_never_disables_the_lock`). Ровно такой тест нужен каждой переменной из
> реестра — образец для остальных.

M-3 («rate-limit выключен в проде»), L-4, L-5, L-10 — один класс: **значение по умолчанию
разошлось с намерением**. Мера: файл `docs/DEFAULTS.md` (или `defaults.toml`) со всеми
переменными `Citadel_*` и флагами установщика: значение, что бывает при отсутствии, кто выставляет
в проде. Тест сверяет список с кодом и **падает при появлении новой переменной**, не описанной в
реестре.

### P-3. Fail-closed как проверяемое правило

M-1, M-2, M-7 закрывались одинаково: «мусор в настройке = отказ, а не тихое отключение защиты».
Мера: параметризованный тест, который для каждой переменной из реестра P-2 подставляет пустое
значение и мусор и требует либо отказа старта, либо документированного безопасного поведения.

### P-4. Реестр форматов и минимальных читаемых версий

Сейчас в дереве живут: bundle **v5** (MIN_READ 4), vault **v4** (назад не читается), obfs **v2**,
кадр эпохи **v2**, реестр с 4-м полем флагов, капсула с необязательным хвостом. Мера: одна таблица
(в `SPEC.md`) «формат → текущая версия → минимальная читаемая → что ломается при откате» и тест
совместимости на каждый формат (для vault и bundle такие тесты уже есть — довести до полного набора).

### P-5. Бюджет секретов

Явный список: какой секрет где живёт, сколько, кто его затирает (`MK`, `client_seed`,
`device_seed`, `admin_seed`, `obfs.master`, ключ эпохи, токены, пароль). Мера: таблица в
`docs/SERVER-KEY-PROTECTION.md` + клиентская часть в `CLIENT-ARCH.md`, и тест из Ф3 про журнал.

### P-6. Регрессии на известные «грабли»

Список ловушек, на которые уже наступали, с тестом или чеклистом на каждую: инвариант `protect`;
права на файлы, открытые ДО `drop_privileges` (`cap_drop: ALL` снял `CAP_CHOWN`); неизменяемый
буфер ответа MethodChannel; DNS-резолвер, который обязан ходить через туннель; `ISSUER_IP` вместо
DNS-имени внутри поднятого туннеля в харнесе. **Половина уже покрыта тестами — нужен сам список,
чтобы новые «грабли» дописывались, а не забывались.**

### P-7. Anti-goals: что мы намеренно НЕ делаем

Чтобы отвергнутые решения не возвращались следующим заходом: per-абонентский PSK (ломает
неразличимость Layer-2), управляющие операции на сервере (`registry revoke|list`), привязка токена
к сессии в рамках blind RSA (недостижима — а в VOPRF достигнута иначе), `DEVICE_CREDENTIAL` как
резерв к биометрии, подпись внутри `citadel://`, keep-alive на exit'е. Каждая строка — с причиной
и ссылкой на разбор.

### P-8. Чеклист приёмки релиза

Один список, объединяющий: гейты CI, живые проверки (`SECURITY-AUDIT-4 §11.1` и `§16`), проверку
прав файлов, SBOM, подпись, и — обязательным пунктом — сверку документации с кодом. Без этого
«живые проверки» так и остаются списком, который никто не открывает в момент выкатки.

### P-9. Календарь ключей и сценарии компрометации

Что ротируется само (ключ эпохи L1 и токены — раз в час), что вручную (идентичность сервера —
переустановка), что не ротируется вовсе (мастер-ссылка до реинсталла). К каждому — сценарий
«что делать, если утекло»: сегодня ответ на большинство вопросов — реинсталл, и это нормально,
но должно быть **написано**, а не выводиться каждый раз заново.

### P-10. Модель угроз как живой документ

`THREAT-MODEL-STRIDE.md` обновляется в том же коммите, что и код, меняющий поверхность
(биометрия и vault v4 — свежий пример: строка про платформенный слот появилась только в этой
правке). Мера: пункт в чеклисте PR — «поверхность изменилась → строка STRIDE обновлена».

---

## 7. Журнал прогонов

| Дата | Ревизия | Что прогонялось | Результат |
|---|---|---|---|
| 2026-08-14 | `9431cfe` | `cargo test --workspace --locked`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo deny --workspace check` | 332 теста / 0 провалов; clippy чист; advisories, bans, licenses, sources — ok |
| 2026-08-15 | заход 12 (аудит-4 §21) | те же три гейта + `flutter analyze`/`flutter test`, `cargo ndk -t arm64-v8a check\|clippy`, `cargo check\|clippy -p citadel-winsvc --target x86_64-pc-windows-gnu`, `./gradlew :app:compileDebugKotlin`, `bash -n` установщика | 338 тестов / 0 провалов; clippy чист; deny ok; `flutter test` 52; Kotlin и Windows-цель собираются |

---

## Приложение A. Цели фаззинга (Ф1)

| # | Цель | Точка входа | Инвариант («падение» = что) | Приоритет |
|---|---|---|---|---|
| F1 | obfs-пакет | `citadel_obfs::open` / `Opener::open` (`lib.rs:296,355`) | не паникует, не аллоцирует по длине из пакета сверх потолка, мусор → `Err` | ★★★ |
| F2 | капсула | `citadel_masque::capsule::decode` (`lib.rs:136`) | `Option`, без паник и переполнений | ★★★ |
| F3 | адресная капсула | `decode_assigned_v4`, `decode_idle_hint` (`:180,213`) | то же; hint не выводит клиента за границы констант | ★★ |
| F4 | IPv4-разбор | `ip::parse_ipv4`, `tcp_dport` (`:305,327`) | не паникует на усечённых/битых заголовках | ★★★ |
| F5 | varint | `varint::decode` (`:36`) | нет переполнения, нет чтения за границей | ★★ |
| F6 | кадр издателя | `citadel_token::read_frame` + `pqid::parse_client_frame` (`lib.rs:157`, `pqid.rs:256`) | длина из кадра не диктует аллокацию, мусор → `Err` | ★★★ |
| F7 | кадр гейта | `parse_gate_frame` (`lib.rs:554`) | то же | ★★ |
| F8 | реестр | `admin::parse_registry` (`admin.rs:145`) | строка любого вида не роняет издателя и не «воскрешает» записи | ★★ |
| F9 | ссылка | `CredentialLink::from_uri` (`creds.rs:347`) | мусорный URI → `Err`, без паник; поля не выходят за диапазоны | ★★★ |
| F10 | хранилище | `Vault::open` по подложенному файлу (`vault.rs:257,538`) | границы Argon2 и таблицы слотов соблюдаются ДО криптографии | ★★★ |
| F11 | **граница привилегий Linux** | `vpnd::valid::parse`, `parse_allow_exits` (`valid.rs:47,89,167`) | ни одна строка не проходит в argv `ip`/`iptables` неразобранной | ★★★★ |
| F12 | **граница привилегий Windows** | `winnet::decode_config`, `parse_stream` (`lib.rs:86,204`) | служба SYSTEM не паникует и не выполняет план из мусора | ★★★★ |

## Приложение B. Команды

```bash
# гейты, которые есть сегодня
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo deny --workspace check
( cd app/rust && cargo deny --config ../../deny.toml check )
bash docker/run-demo.sh && bash docker/run-cli-tests.sh   # заканчивать: docker compose down

# Ф1 (после cargo install cargo-fuzz)
cargo +nightly fuzz run capsule_decode -- -max_total_time=60
cargo +nightly fuzz run vpnd_valid_parse -- -max_total_time=600

# Ф2
cargo +nightly miri test -p citadel-obfs -p citadel-masque
RUSTFLAGS="-Zsanitizer=address" cargo +nightly test -p citadel-tun --target x86_64-unknown-linux-gnu

# Ф0/Ф5
gitleaks detect --no-banner
cargo cyclonedx --format json
```

## Приложение C. Чеклист PR (предлагается в шаблон)

- [ ] Появился новый разбор недоверенного ввода? → цель фаззинга в `fuzz/` и запись в Приложение A
- [ ] Появилась новая переменная `Citadel_*` или флаг установщика? → строка в реестре дефолтов (P-2)
      и негативный тест на мусорное значение (P-3)
- [ ] Изменилась поверхность атаки? → строка в `THREAT-MODEL-STRIDE.md` тем же коммитом (P-10)
- [ ] Сломан формат провода или файла? → таблица версий (P-4) и раздел «что требуется при выкатке»
- [ ] Новый `unsafe`? → `SAFETY`-комментарий отвечает «почему верно», а не «что делает»
- [ ] Новый исходящий сокет в движке? → маршрут выбран явно (`Route`), см. P-1
- [ ] Секрет пересекает новую границу? → кто затирает копию, отражено в бюджете секретов (P-5)
