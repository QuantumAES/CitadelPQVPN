# CitadelPQVPN — стратегия разработки после security-аудита

**Дата:** 2026-07-06 · **Основание:** аудит red-team/DevSecOps (см. память проекта `citadel-security-audit-2026-07`) · **Оптика:** инженерный триаж находок + пересборка дорожной карты.

Документ не заменяет `SPEC.md`/`THREAT-MODEL-STRIDE.md`, а надстраивает над ними приоритеты. Существующие роадмапы (`SPEC §12` M0–M7, `CLIENT-ARCH` C0–C6) остаются; здесь — как их пересеквенсировать в свете аудита.

---

## 0. Ре-базлайн статуса (честная метка «готовности»)

`SPEC §12` помечает M0–M7 как ✅. Аудит показал, что корректная метка — **PoC ✅ / production ❌**. Конкретно:

- **M4 (TCP-fallback)** — не production: канал держится на статическом общем PSK, без TLS/PQ/PFS (H1). SPEC обещает «QUIC-in-TCP» — не реализовано.
- **M7 (PQ-auth)** — не production: в дефолтном деплое выключена; при отсутствии pin деградирует в подпись над константой (H2).
- **M2 (kill-switch)** — сам SPEC честно держит 🟡; аудит подтверждает fail-open при крахе (M9).

**Вывод стратегии:** протокол — PoC-grade. Клиентский трек (C*) вырвался вперёд и «выглядит готовым к отгрузке», тогда как транспорт под ним имеет критическую дыру. **Правило:** никакой публичной/бета-экспозиции, пока не закрыт S0 (блокеры) и не проведён мини-аудит диффов.

---

## 1. Инженерный триаж аудита

Легенда: **Эффорт** S/M/L · **Wire** — ломает ли формат провода/тест-векторы · **Куда** — в какой этап садится.

| # | Находка | Вердикт инженера | Эффорт | Wire | Куда |
|---|---|---|---|---|---|
| **H1** | TCP-fallback = статический PSK, без PFS/PQ | **Подтверждаю блокер.** Правильный фикс — не «второй протокол», а `AsyncUdpSocket` поверх TCP, несущий тот же QUIC(TLS1.3+hybrid). Схлопывает дублирование `Tunnel::Tcp` → чистый выигрыш. | L | **да** | S0.3 |
| **H2** | Insecure-by-default при отсутствии pin | **Подтверждаю блокер, но дёшево.** fail-closed + привязка ML-DSA к живому серту. | S | нет | S0.1 |
| **H3** | Нет фильтра inner-source → спуфинг; IPv6 fail-open | **Подтверждаю.** IPv4-спуфинг — острый; IPv6 — латентный (в дефолте IPv6-forward off). Есть дешёвый стопгэп (rp_filter + `-s pool`). | S→M | нет | S0.2 |
| **M1** | dns/routes-инъекция из ссылки в root-helper | **Понижаю до гигиены.** Автор ссылки уже владеет коннектом (pin+psk+server) → мог бы задать свой резолвер легально. Реальная суть — граница app↔helper: helper не должен доверять строкам app. Фиксить, но не блокер. | S | нет | S1.2 |
| **M2** | obfs nonce-reuse (64-бит sid + общий PSK) | ✅ **ЗАКРЫТ (obfs v2, 2026-07-21):** sid 8→16 байт = per-session соль в `k_sess` (коллизия ключа 2⁻¹²⁸). Слом wire выполнен, KDF-контексты `obfs/v2/*`. Ранее A8 давал частичный митигейт (random packet_id). | S(частич)/M(полн) | сделан | S0.3 |
| **M3** | Нет anti-replay; SPEC обещает | **Сначала чинить СПЕКУ, не код.** Транспорт шлёт только TYPE_DATA; Phase-0 gate-хендшейк не нужен (лишняя поверхность). Probe-resistance держит AEAD-гейт. Replay-кэш — низкий приоритет. | S(спека) | нет | S0.3/S3 |
| **M4** | PQ-downgrade не форсится | **Понижаю: политика, не атака.** MITM не понизит pq-only клиента (TLS-транскрипт). Суть — installer `KX=all` + нет ассерта «группа=гибрид». Дёшево. | S | нет | S1.1 |
| **M5** | Дефолт без токенов/ML-DSA | **Не баг — посадка v1.** Bearer-link легитимен (как algo/wg), ЕСЛИ документирован. Апгрейд уже запланирован (C5). Переклассифицирую в roadmap-решение. | — | нет | C5 |
| **M6** | Double-spend per-exit, in-RAM | **Субсумируется C5** (epoch-registry на issuer). До C5 токенов в проде нет → moot. | — | нет | C5 |
| **M7** | Нет zeroize | Гигиена, дёшево. rustls/aws-lc свои ключи чистят; наши копии — нет. | S→M | нет | S1.3 |
| **M8** | Персистентный plaintext-лог клиента | Реальная приватностная дыра, дёшево. Прямо противоречит no-logs. | S | нет | S1.4 |
| **M9** | Нет fail-closed kill-switch | Реально; **уже в плане C6**. Нетривиально (переживать краш движка). | M | нет | C6 |
| L1–L5 | Argon2id / supply-chain / ro-rootfs / SPKI-pin / RSA-DoS | Хардненинг. Argon2id→C6; supply-chain→S1.5; SPKI-pin — при следующем сломе wire. | S each | — | S1.5/C6 |

**Ключевые расхождения с аудитом (добавленная инженерная ценность):**
1. **H1+M2+M3 — это один wire-ломающий релиз, не три.** Развёрнутых пользователей нет → ломаем формат один раз: QUIC-over-TCP + соль/увеличенный sid + регенерация PHASE0-векторов + правка спеки. Не размазывать сломы по релизам.
2. **M1/M4 переоценены аудитом** (гигиена/политика, не критикал) — но всё равно чинятся, дёшево.
3. **M5/M6/M9 — не новая работа: они уже C5/C6.** Аудит не изобрёл их, а провалидировал приоритет клиентского трека.
4. **H1-фикс — это упрощение, а не только «безопасность»:** после него единый QUIC-путь, obfs — чистая транспорт-обёртка (UDP или TCP), `Tunnel`-дуализм в control/pump уходит.

---

## 2. Предпосылки (Gate 0 — до старта security-трека)

Security-пуш на 40+ юнит + 15 docker-тестов без страховки = гарантированные регрессии. Поэтому **сначала**:

- **G0.1 CI.** Сейчас `.github/workflows` пуст (remote `QuantumAES/CitadelPQVPN` живой). Поднять workflow: `cargo test --workspace` + `cargo clippy -D warnings` (тулчейн 1.96) + прогон docker-харнеса. Без CI security-трек не ведётся.
- **G0.2 Чистое дерево.** Закоммитить in-flight (android/client/quic/helper + новый diag) до зелёного (workspace + docker 15/15). Security-фиксы должны стартовать с бисектируемого базлайна.
- **G0.3 Снимок wire.** Зафиксировать текущий формат/тест-векторы obfs — чтобы осознанно ломать их в S0.3.
- **G0.4 Fuzzing (опц., но к месту).** rustup стоит → завести `cargo-fuzz` на парсеры (obfs/masque) как гейт S0. Парсеры = поверхность атаки.

---

## 3. Обновлённая дорожная карта

### S0 — Блокеры релиза (гейт публичной/бета-экспозиции)
- **S0.1 (H2) fail-closed pinning.** Без pin — отказ (кроме `--insecure` dev-флага). ML-DSA: требовать активный pin + привязка к серту живой сессии, а не к провижининг-константе. *Тест:* негатив в харнесе (no-pin → refuse; чужой pin → refuse).
- **S0.2 (H3) анти-спуфинг.** Стопгэп: `rp_filter=1` + `iptables FORWARD -i tun ! -s pool -j DROP` в `server_setup_net`. Затем app-layer: `src == назначенный` в `Inbound::accept` + default-deny не-IPv4. *Тест:* спуфнутый src / IPv6 дропаются.
- **S0.3 (H1+M2+M3) унификация транспорта.** `AsyncUdpSocket`-over-TCP несёт настоящий QUIC(TLS1.3+hybrid); удалить `Tunnel::Tcp`-спецпуть. В этом же wire-ломающем релизе: per-session соль в `k_sess` (+ увеличить sid), регенерация PHASE0-векторов, привести SPEC (§7/§11 «QUIC-in-TCP», §4 anti-replay) в соответствие. *Тест:* fallback показывает PQ-хендшейк; PSK-only не расшифровывает трафик; TEST 11 переписан.
  - **Статус (2026-07-21): H1 ✅, M3 ✅, A8 ✅, M2-full ✅ ЗАКРЫТ (obfs v2).** `sid` расширен 8→16 байт и служит 128-битной per-session солью в `k_sess` (одно поле закрывает «соль в KDF» и «больше sid»: коллизия ключа 2⁻¹²⁸ → body-AEAD nonce-reuse под общим PSK устранён by-construction). KDF-контексты подняты `obfs/v1`→`obfs/v2` (взаимно нерасшифровываемы). Сделано: `tools/obfs_ref.py` перегенерён (v2), `citadel-obfs/lib.rs` под эталон (3 байт-вектора + KDF), потребители (`obfs_socket`/`tcp_obfs`/`obfs_stream`/`obfs_tcp`) на 16-байтный sid, доки PHASE0-OBFS/SPEC §5.1. **147 тестов + clippy зелёные.** Docker-харнес не требует правки (нет byte-ассертов obfs, один бинарь на оба конца). **Осталось операционно:** согласованный re-release (`mk-release`→`publish-release`) + переустановка сервера (ротация §3.5 и так бьёт старые ссылки) + сборка нового клиента + раздача новых ссылок. **НЕ закоммичено — за пользователем `-S`.**

**Критерий выхода S0:** docker-харнес зелёный на новых негативах; fallback криптографически эквивалентен UDP-пути; мини-аудит диффов.

### S1 — Хардненинг до широкой беты
- **S1.1 (M4)** PQ-only: installer default `KX=pq`; клиент ассертит negotiated-группу = гибрид под pq-политикой; `all` — только явное окно миграции.
- **S1.2 (M1)** валидация входов + граница app↔helper: `dns`=один IP, `routes`=CIDR (при импорте И в helper); не интерполировать неподтверждённые строки в конфиги.
- **S1.3 (M7)** zeroize на PSK/seed/токены/vault-key/расшифрованные профили.
- **S1.4 (M8)** приватность лога: opt-in/ring-only/в vault; очистка на disconnect.
- **S1.5 (L2–L4)** supply-chain: pin Docker-инсталла + digest-pin base-image; reproducible builds; cosign в дополнение к minisign; `read_only: true` + tmpfs у контейнера.

### S2 — Свёрнуто в клиентский трек (уже запланировано)
- **C5 (идентичность)** = продакшенизация auth: двухслойные epoch-токены + issuer-registry (закрывает **M5** per-user + **M6** persistent double-spend); завести токены+ML-DSA в installer.
- **C6 (упаковка)** = fail-closed kill-switch (desktop-firewall + Android `setBlocking`/always-on, закрывает **M9**) + нотаризация + **Argon2id** для vault (L1).
  - **Известное ограничение (2026-07-12, боевой тест APK):** реконнект при смене сети (toggle WiFi) работает только на **переднем плане**. Причина — детект-сети (NetworkCallback) и цикл реконнекта привязаны к Activity/Flutter-движку; при уходе app в фон (а именно так пользователь переключает WiFi) Activity гасится → событие сети теряется, движок не крутит ретраи, и передний план это не восстанавливает (событие уже прошло). Фикс архитектурный и входит в C6 «always-on»: увести детект-сети + реконнект (и сам движок) в **нативный `CitadelVpnService`** (переживает смерть Activity) либо в фоновый Flutter-isolate. Тестируется только на устройстве. Первый Dart-driven срез (2026-07-05) закрывает foreground-случай.

### C7 — Admin-plane v2: выдача/отзыв доступа по туннелю (спроектирован 2026-07-17; заменяет SSH-путь GUI из C5.5)

**Мотивация.** Управление реестром абонентов из GUI сейчас идёт по SSH (`api/admin.rs` → russh → `citadel-token registry` на сервере): (а) в GUI вводятся **root-креды сервера** — чрезмерная привилегия для операции «добавить абонента» и лишний долгоживущий секрет в приложении; (б) host-key — TOFU без персиста (MITM first-use); (в) russh не собирается в мобильный APK → функция desktop-only; (г) UX — отдельный экран с SSH-полями вместо меню серверов. Одновременно «админская ссылка» инсталлера — обычная абонентская: понятия «мастер-ссылка» в формате кред нет, роль админа держится на знании SSH-пароля.

**Дизайн — admin-роль поверх существующего PQ-TLS канала issuer** (та же машинерия, что S2.1/A1 + Layer-1):

1. **Admin-идентичность.** Инсталлер генерит отдельный Ed25519 `admin.seed`; на сервере (том issuer) остаётся ТОЛЬКО pub (`admin_id`), seed уходит в мастер-ссылку. Формат кред **v3**: поля `admin_seed` (+ `admin_port`, дефолт 7001) с `#[serde(default)]` → v2-ссылки продолжают читаться (admin_seed=None). Мастер-ссылка = обычная абонентская + admin_seed; клиентские ссылки этих полей **не несут**.
2. **Протокол.** Тот же PQ-TLS (пин из ссылки, X25519MLKEM768): после серверного challenge админ отвечает кадром `0x01 ‖ pub(32) ‖ sig(64)`, где `sig = Ed25519(admin_seed, "citadel-admin/v1" ‖ challenge ‖ EKM)`, EKM — TLS exporter сессии. Domain-separation отделяет admin-auth от Layer-1-auth (кросс-протокольный replay подписи невозможен: ключи разные И контексты разные), EKM привязывает подпись к конкретной TLS-сессии (channel binding — закрывает A3-класс для admin-плоскости). Абонентский кадр остаётся 96 Б → wire аддитивен, старые клиенты не затронуты. Дальше — CBOR-кадры команд `list` / `add(client_id, valid_until)` / `revoke(client_id)`; правки реестра — существующими атомарными `registry_apply_add/revoke` (вынести из main.rs в lib).
3. **Достижимость — только из туннеля.** Admin-listener на отдельном порту (:7001) **без публикации наружу**: compose-порт не публикуется; exit добавляет DNAT `10.7.0.1:7001 → issuer:7001` только для пакетов с `-i Citadel0`. Итого admin-плоскость не видна WAN-сканам, а команды идут в **трёх** слоях защиты: PQ-QUIC туннель → PQ-TLS с пином → admin-подпись. Публичным остаётся только :7000 (token-fetch, ему это необходимо: токен нужен ДО туннеля).
4. **Выдача доступа новому абоненту — целиком на устройстве админа.** Ядро генерит свежий `client_seed` (CSPRNG) → по admin-каналу регистрирует только pub (client_id) → клиентскую ссылку собирает **локально** (бандл админа минус admin_seed, с новым client_seed) → QR/копирование. Issuer seed нового абонента не видит (модель C5.4b сохранена). Метки «кому выдано» — локально в vault админа, **не на сервере** (не заводим серверную БД деанонимизации).
5. **Enforcement — на сервере; UI-гейтинг — вторичен.** Пункт «Абоненты» рендерится только для admin-профиля (`isAdmin` = наличие admin_seed), но реальный барьер — проверка `admin_id` на issuer: клиентская ссылка не даёт admin-операций даже из модифицированного приложения.
6. **Все платформы.** Admin-клиент — тот же sync-rustls код, что `fetch_tokens` (`spawn_blocking`) → Android/iOS/десктопы без russh. SSH-экран (`AdminRegistryPage`) и russh-бэкенд `api/admin.rs` удаляются из app; russh остаётся только в `citadel_client::deploy` (C4, вне GUI). Break-glass — bootstrap-скрипт по SSH руками на сервер (как при установке).

**Риски и митигейты:**

| # | Риск | Митигейт |
|---|---|---|
| R1 | Кража мастер-ссылки = контроль admin-плоскости | vault-шифрование + zeroize; канал только из туннеля; UI-бейдж «Admin» и предупреждение «не раздавать»; ротация admin.seed через break-glass SSH |
| R2 | Эскалация клиент → админ | Протокольно невозможна: `admin_id` проверяет сервер; скрытие меню — только UX |
| R3 | Кросс-протокольный replay подписи (Layer-1 ↔ admin) | Разные ключи + domain-separation `"citadel-admin/v1"` + EKM channel-binding |
| R4 | MITM/replay admin-команд | Pinned PQ-TLS (fail-closed) + fresh challenge per-connection + EKM |
| R5 | DoS/скан admin-порта | Порт недостижим извне туннеля (доступ уже требует валидной VPN-сессии); throttle неуспешных auth |
| R6 | Self-lockout (админ отозвал собственный client_id) | Issuer отказывает revoke для client_id из `admin.client_id` (пишет инсталлер); break-glass SSH |
| R7 | Гонка записи реестра (admin-канал ∥ CLI) | Атомарный tmp+rename уже есть; last-writer-wins приемлем для однофайлового реестра (документировать) |
| R8 | Приватность метаданных абонентов | На сервере только pub+срок+статус (как сейчас); имена/метки — только в vault админа |
| R9 | Совместимость розданных ссылок | v3 читает v2 (`serde(default)`); issuer-wire аддитивен (96 Б = абонент, 97 Б = админ) |

**Этапы:**
- **C7.1 citadel-token: admin-протокол — ✅ (2026-07-17).** Registry-ops вынесены в `citadel_token::admin` (общие для CLI и канала); отдельный admin-listener (`Citadel_ADMIN_LISTEN`, не смешан с :7000 — там по-прежнему только 96-байтовый Layer-1 кадр) с `AdminServer::serve_conn`: challenge → кадр `0x01‖pub‖sig` (домен `citadel-admin/v1` + EKM `EXPORTER-citadel-admin/v1`) → CBOR-команды; чтение `admin_id`/`admin.client_id` на каждую операцию (ротация без рестарта); throttle 1с на провал auth, отказ без ack (не оракул); `AdminClient` (sync, для C7.3). *Тесты (9):* e2e list/add/revoke + lockout-guard + дефолтный срок + «прошлое → отказ»; негативы — чужой ключ, отсутствие admin_id (secure default), Layer-1-кадр (даже если ключ = admin_id), подпись без домена/без EKM/чужой EKM/подделка; CBOR roundtrip; parse_registry.
- **C7.2 креды v3 + installer — ✅ (2026-07-17).** `admin_seed`/`admin_port` в `CredentialBundle`/`CredentialLink` (`#[serde(default)]`, `BUNDLE_VERSION=3`, чтение v2..=v3 → старые ссылки живы; zeroize admin_seed; `is_admin()`/`admin_port()`); `citadel-linkgen --admin-seed/--admin-port` (+предупреждение о мастер-ссылке). Data-plane: `Inbound::with_admin` пропускает TCP к `(admin_vip, admin_port)` мимо F2 (после анти-спуфинга src) → ядро DNAT'ит на issuer; `ip::tcp_dport`; `pump` получил `admin_dst`; m1 читает `Citadel_ADMIN_VIP/PORT`, ставит DNAT `-i Citadel0` (наружу порт не публикуется). Инсталлер: keygen `admin.seed`, `admin_id`+`admin.client_id` на том issuer, `Citadel_ADMIN_LISTEN` на issuer, VIP/DNAT (резолв issuer по docker-DNS) на exit, отдельные мастер- и клиент-ссылки. `ADMIN_VIP` — константа (= шлюз `Citadel_TUN_ADDR`, инвариант деплоя). *Тесты (+3):* admin-поля round-trip + accessors, **v2-ссылка парсится под v3** (доказывает map-кодирование ciborium), data-plane admin-исключение (только TCP+точный порт+VIP+назначенный src). *Отложено в C7.5:* nmap-негатив «порт снаружи закрыт» — в docker-харнесе. *Известное огранич.:* DNAT резолвит issuer-IP один раз на старте exit — рестарт issuer с новым IP требует рестарта exit (как и pin-файлы).
- **C7.3 citadel-client: admin-ядро + минт ссылок — ✅ (2026-07-18).** Модуль `citadel_client::admin` (async поверх `spawn_blocking`/`citadel_token::admin`, БЕЗ cfg-гейта → все платформы, в отличие от russh): `admin_list`/`admin_revoke`/`admin_issue` выводят адрес (`ADMIN_VIP:admin_port`), pin (`issuer_pin`) и `admin_seed` из МАСТЕР-ссылки — недоступны с клиентской (fail-closed). `build_subscriber_link` — чистая сборка клиентской ссылки: свежий CSPRNG-seed, admin-поля вырезаны (+zeroize old admin_seed). `admin_issue` = seed→client_id→сборка ссылки (ДО регистрации)→`add` по каналу. Хелперы `parse_client_id`/`parse_valid_until`. Vault: `IssuedRecord` + `add_issued`/`list_issued`/`remove_issued` (метки «кому выдал» — только на устройстве админа; `#[serde(default)]` → старые vault читаются). *Тесты (+6):* e2e issue→list→revoke против in-process issuer admin-сервера, отказ на чужой admin-seed, strip-admin+swap-seed минта, вывод conn-параметров + отказ не-мастер, parse_valid_until, vault issued upsert/roundtrip. *Прим.:* admin-канал достижим только из-под поднятого туннеля (ADMIN_VIP=шлюз /16 → on-link и в split-tunnel). Каждая операция — самодостаточный connect→op→close.
- **C7.4 FFI + GUI — ✅ (2026-07-19).** FFI `api/admin.rs` ПЕРЕПИСАН с SSH (`AdminConn`/russh) на туннельный admin-канал: `admin_subscribers`/`admin_issue_subscriber`/`admin_revoke_subscriber` (единый бэкенд ВСЕХ платформ — admin-режим появился и на мобильных); метки из vault подмешиваются в листинг (`SubscriberDto.label`), выдача пишет метку best-effort ПОСЛЕ регистрации, отзыв метку сохраняет (история «кому был выдан»). `ProfileDto.is_admin`/`LinkSummaryDto.is_admin` (+`CredentialSummary.is_admin` в ядре); `link_qr` — QR битовой матрицей (`to_qr_matrix`), рендер в Dart кастомным painter'ом (без новых Dart-зависимостей). GUI: пункт «Абоненты» в меню admin-профиля + чип admin, экран `SubscribersPage` (гейт «нужна активная сессия этого профиля» + автозагрузка при поднятии туннеля; список с метками; выдача: метка+срок → one-time показ ссылки QR/копированием с предупреждением «повторно не восстановить»; отзыв с конфирмом), предупреждение «мастер-ссылка» в превью добавления; `AdminRegistryPage` (SSH-экран) и SSH-пункт настроек удалены. Хардненинг: `AdminClient::connect` получил connect-timeout 10с + I/O-timeout 15с (VIP без туннеля = SYN-blackhole → раньше висело бы до ~2 мин). *Тесты (+2):* сводка видит admin-ссылку; QR-матрица согласована (size², 0/1, оба цвета). *Прим.:* `citadel_client::deploy` (russh) остаётся для C4-деплоя; из admin-пути russh исключён.
- **C7.5 e2e + доки — ✅ (2026-07-19).** CLI `citadel-token admin <list|add|revoke>` — операции ПО СЕТЕВОМУ admin-каналу (тот же путь, что GUI; env `Citadel_ADMIN_ADDR`/`Citadel_ISSUER_PIN`/`Citadel_ADMIN_SEED`; ops/break-glass с любой машины с мастер-кредами и туннелем; `registry` остаётся оффлайн-правкой на сервере). Демо-стек получил admin-плоскость (issuer: `Citadel_ADMIN_LISTEN=:7001` + `admin_id`/`admin.client_id`; exit: VIP/PORT/DNAT как в installer). *Docker-харнес +3 теста (все зелёные с первого прогона, 15/15 старых без регрессий):* **ТЕСТ 20** — list по туннелю; add нового абонента по каналу → он получает epoch-токены (допущен к exit); revoke по каналу → отказ (≤ эпохи); **ТЕСТ 21** — клиентский (валидный Layer-1) ключ в admin-кадре → отказ (домен-разделение auth); self-revoke client_id админа → отказ (R6); **ТЕСТ 22** — TCP к admin-порту на внешнем интерфейсе exit закрыт (DNAT только `-i Citadel0`). Audit-trail admin-мутаций виден в `docker logs issuer`. *Доки сверены:* SPEC §8.1 (admin-плоскость: канал/роли ссылок/guard-rails), CLIENT-ARCH §6/§8/§10 (GUI «Абоненты», статус деплоя, admin-плоскость + дельты кода), THREAT-MODEL §5 (STRIDE admin-канала, актив K7, поток (7), верификация).

**Гейт:** C7 не добавляет публичной поверхности (admin-плоскость за туннелем; наружу по-прежнему только 4433/udp, 443/tcp, 7000/tcp) — подтверждено харнесом (ТЕСТ 22). Слома wire нет — правило «один слом wire в S0.3» не нарушается. **Трек C7 закрыт целиком (C7.1–C7.5 ✅, 2026-07-17..19).**

> **Нумерация.** C7 = admin-plane v2 (реализовано) — единая с `CLIENT-ARCH §14`. Прежний трек «Сетевой контроль User-mode» (split-tunnel / killswitch / DNSSEC, ещё не начат) переименован в **C8** (был одноимённым C7 в CLIENT-ARCH — коллизия разведена 2026-07-20).

### S2-hardening — второй аудит (A1–A9, 2026-07-15..19)

Второй red-team/DevSecOps-аудит (память `citadel-security-audit2-2026-07`) подтвердил S0/S1 закрытыми и дал 9 находок по C5/C6-поверхности. **Все закрыты в коде + docker-харнесе.** Сводка (детали и тест-привязки — в памяти и `THREAT-MODEL §5` для admin-канала):

| ID | Находка | Severity | Фикс | Статус |
|---|---|---|---|---|
| **A1** | issuer-канал plaintext+неаутентифицирован (MITM-кража токенов, деанон client_id, импёрсонация) | HIGH | S2.1: issuer за PQ-TLS 1.3 (гибрид) + пиннинг серта (BLAKE3 DER); `fetch_tokens`/admin требуют pin (fail-closed). **Остаток (2026-07-19):** синхронная **obfs-обёртка** token-/admin-каналов (`citadel_token::obfs_stream`, тот же PSK, что туннель) — TLS-хендшейк на проводе не виден, issuer-порт молчит на не-obfs пробу и неотличим от туннеля | ✅ |
| **A2** | IPv6-утечка мимо IPv4-туннеля/KS (deanon на dual-stack) | HIGH | S2.2: `citadel-helper` `ip6tables` KS-цепочка + IPv6:53 блок; Android `VpnService` `::/0`-blackhole. Linux ✅; Android — на устройстве | ✅ (Linux) / device-pending |
| **A3** | M7 ML-DSA без channel-binding (relay-MITM под CRQC) | MED | S2.6: `bind_msg` += TLS-exporter (RFC 5705) — два TLS-плеча дают разные exporter'ы → подпись не проходит | ✅ |
| **A4** | exit-хост достижим из туннеля (пивот на SSH/issuer/published-порты) | MED | S2.3: `INPUT -i <tun> DROP`. **Усилен (2026-07-19):** `-A` → `-I INPUT 1` (fail-closed поверх существующего firewall) | ✅ |
| **A5** | TCP-fallback DoS (безлимитные pre-auth endpoints) | MED | S2.5: семафор 256 in-flight + таймаут хендшейка 10с. **Усилен (2026-07-19):** throttle лога отклонений (анти лог-амплификация) | ✅ |
| **A6** | нет квоты токенов на подписчика (фарм фрирайдеров) | MED | S2.4: `quota_grant` per-(client_id,эпоха), env `Citadel_TOKEN_QUOTA` (default 64) | ✅ |
| **A7** | exit регенерит cert/pin+ML-DSA на рестарт (розданные ссылки мрут) | LOW | персист идентичности (`ServerSigner::from_seed`, `Citadel_KEY_DIR`) | ✅ |
| **A8** | M2-митигейт (случайный packet_id) только на TCP | LOW | `obfs_socket send_ctr = rand::random()` и в UDP-пути | ✅ |
| **A9** | над-claim'ы в доках (anti-replay, SPKI-pin, TLS-session) | LOW/doc | правки SPEC §4 / creds / pqauth-комментов | ✅ |

**Гейт S2:** BUNDLE_VERSION 1→2 (issuer_pin) — единственный слом issuer-канала; obfs-обёртка (A1-остаток) слома формата ссылок НЕ несёт (PSK уже в v2/v3), но требует **согласованного обновления сервера и клиента** (обе стороны включают obfs по наличию PSK; розданные ссылки валидны). Env-добавки: `Citadel_ISSUER_PIN`, `Citadel_TOKEN_QUOTA`, `Citadel_KEY_DIR`, `Citadel_OBFS_PSK` (issuer). Остаётся device-тест A2 (Android IPv6-blackhole).

### S3 — Сверка доков с кодом (непрерывно)
По мере фиксов приводить в соответствие claims: SPEC (QUIC-in-TCP, anti-replay, KX-policy), PHASE0 (векторы), creds (SPKI-pin vs blake3-DER), THREAT-MODEL (статусы S4/E1/kill-switch), CLIENT-ARCH.

---

## 4. Инженерные guardrails

- **Тест на каждый фикс.** Security-фикс без red→green теста не мёржится. Харнес растёт 15 → ~20+.
- **Один слом wire.** Все формат-ломающие изменения — только в S0.3. Не дробить.
- **Гейт экспозиции.** Публичный endpoint / бета-инвайты — только после S0-green + мини-аудит.
- **Инвариант «один движок».** После S0.3 — единый QUIC/TLS-путь; obfs = транспорт-обёртка (UDP|TCP), не параллельный протокол.
- **Честные метки.** `SPEC §12` перевести на «PoC ✅ / prod ⏳» по M4/M7; `THREAT-MODEL` S4 → в работе (S0.2).
- **Тулчейн-риск.** ML-DSA на `aws-lc-rs unstable` — пинить версии, следить за стабилизацией API.

---

## 5. Одной фразой

Протокол — PoC, а не продукт. **Заморозить расширение клиентской ширины, вставить security-гейт S0 (H1/H2/H3) перед любой экспозицией, батчить единственный слом wire в S0.3, а M5/M6/M9 добить штатными C5/C6.** Предусловие ко всему — CI и чистое дерево.

---

## 6. Статус трека (2026-07-20)

**Всё ниже закоммичено на `dev` и подтверждено LIVE (боевой VPS + устройство).**

- **S0 блокеры:** S0.1 ✅ (fail-closed pin), S0.2 ✅ (анти-спуфинг), S0.3 ✅ — H1/M3/**M2-full** ✅ (obfs v2: 16-байтный sid = per-session соль в `k_sess`; см. §S0.3). **Весь S0 закрыт в коде.**
- **S1 хардненинг:** S1.1–S1.5 ✅.
- **Аудит-2 (A1–A9):** все ✅ (см. §«S2-hardening»). A1 закрыт полностью, включая obfs-обёртку issuer-канала (probe-resistance).
- **C5 идентичность:** ✅ (epoch-токены, issuer-registry, commitment-fetch).
- **C7 admin-plane v2:** ✅ ЗАКРЫТ (C7.1–C7.5; реестр по туннелю, GUI «Абоненты», CLI, russh удалён).
- **Пакет фиксов 2026-07-19..20:** коллизия C7→C8 разведена; **ротация идентичности сервера при обновлении** (installer §3.5 — обновление инвалидирует все ссылки; `docker restart` не ротирует; opt-out `CITADEL_KEEP_KEYS=1`); ссылки не пишутся на диск VPS; **single-session вариант B** (issuer-lease `Citadel_TOKEN_LEASE_SECS`, мягкий, unlinkability цела); QR-скан камерой; персист режима отладки; UX-фиксы.

**M2-full (задача 6) ✅ ЗАКРЫТ в коде (2026-07-21, obfs v2)** — см. §S0.3. Осталось операционно: согласованный re-release + переустановка сервера + новый клиент + раздача новых ссылок (слом wire; ротация §3.5 и так инвалидирует старые ссылки). **Трек C8 (сетевой контроль), статус 2026-07-21:**
- **C8.3 split-tunnel ✅**: Android — обе оси (приложения `addAllowed/DisallowedApplication` + назначения домен/IP/CIDR через `addRoute`/`excludeRoute`); **Linux — ось назначений** (helper `--bypass` + `gui_tun::split_routes`; Include→в туннель только их, Exclude→в обход через физ.шлюз). Ось приложений на Linux — отложена (решение пользователя: «пока только по назначению»; план — cgroup2+fwmark+launcher).
- **C8.2 ✅** Linux window-close prompt (`window_manager`, «Оставить в фоне»=minimize / «Отключить и выйти»=clean disconnect).
- **C8.5 ✅** запрет скриншотов приложения (Android `FLAG_SECURE`, дефолт ВКЛ): `onCreate` ставит флаг по умолчанию + MethodChannel `setSecureFlag`; персист `screenshot_block` рядом с vault (дефолт true). Desktop не enforce'ит (тумблер Android-only).
- **C8.1 отменён** (свёрнут в C8.3 — KS не трогаем).
- Осталось: **C8.0** хард-делит абонента (`AdminRequest::Remove`); Linux per-app; Win/macOS split/screenshot-block. Мелочь: device-тест QR/lease + split-tunnel/скриншот-блок (Android) / desktop-тест C8.2/C8.3-Linux.

**Верификация на сегодня:** 147 юнит-тестов + `clippy --workspace --all-targets` + `flutter analyze` чисто; docker-харнес 21/21.

**Операционный урок (2026-07-20):** installer тянет бинари из GitHub Release — обновление сервера ТРЕБУЕТ переиздания релиза (`mk-release`→`publish-release`), иначе старый серверный `citadel-linkgen` молча игнорирует новые флаги (наивный пар-парсер), и функции (напр. admin-поля в мастер-ссылке) не появляются, хотя код в репозитории верный.

---

## 7. Трек L — консольный клиент Linux (начат 2026-07-25)

Отдельная модель угроз и решения — **`LINUX-CLI.md`** (L1–L20). Здесь только связь со сквозным
security-треком:

- **Разделение привилегий доведено до Linux-десктопа.** Раньше движок на Linux работал в GUI-процессе
  (привилегии держал `citadel-helper`); в консольной модели весь недоверенный сетевой ввод ушёл в
  отдельный процесс `citadel-engine` под системным uid без capabilities и `NO_NEW_PRIVS` (L13).
  Теперь три платформы согласованы: сервер (drop root→nobody, F4), Windows (служба+приложение, W2),
  Linux (демон+движок).
- **Граница привилегий усилена относительно `citadel-helper`:** вместо «проверить строку предикатом
  и передать дальше» — типизированный разбор с пересборкой аргументов (`valid::TunSetup`), плюс
  проверка охвата: «в обход» шире `/8` отвергается как скрытое отключение kill-switch (L2).
- **Найдено и закрыто при проектировании (L11-bis):** армированный kill-switch блокировал сам движок,
  из-за чего реконнект после аварийного разрыва не поднимался никогда. Введено `AllowExits` —
  точечное открытие доступа к exit/issuer до `TunSetup`, с валидацией адресов. Тот же класс проблемы
  теоретически касается GUI-пути (helper) — **проверить на Linux-GUI и Windows** (issuer на отдельном
  адресе; в текущем деплое issuer живёт на IP exit'а, поэтому симптом не проявлялся).
- **Права файла хранилища (L7, сквозная правка):** `vault.bin` создавался с `0644` — шифртекст был
  доступен любому локальному пользователю для офлайн-перебора. Теперь `0600` + каталог `0700` + тест.
  Правка общая для GUI и CLI.
- **Осознанно НЕ сделано (требует явного решения по продукту):** автостарт сессии до логина —
  `systemd-ask-password` или TPM2-запечатывание (L9); аутентификация заголовка vault (AAD) против
  отката/подмены файла (L7-остаток); интеграция с NetworkManager вместо прямой записи `resolv.conf`
  (L14).

## 8. No-logs на серверных ролях (2026-07-25)

**Проблема.** Exit и issuer писали в stderr (→ `docker logs`/journald, переживает контейнер) ровно
те данные, ради ненаблюдаемости которых построена вся схема анонимных токенов: IP клиента и время
подключения, выданный туннельный адрес, `nonce` предъявленного токена, **назначения пакетов
пользователя** (`[exit] F2: заблокирован inner-dst …`), `client_id` абонента при Layer-1-авторизации
и при admin-мутациях. Связка «IP ↔ client_id ↔ время» восстанавливалась из логов тривиально —
это делало unlinkability (blind RSA, C5.x) декоративной.

**Решение.** Серверные роли по умолчанию **молчат**. Всё, что идентифицирует абонента, его адрес,
назначение или объём трафика, пишется только при явном `Citadel_DEBUG_LOG=1` (макросы
`citadel_quic::dlog!` / `citadel_token::dlog!` поверх `debug_logs()`). Остаются стартовые строки
конфигурации (порты, включённые механизмы) — в них нет пользовательских данных.

- **Exit:** подключение клиента, выданный адрес, приём токена, отказ в доступе, дропы фильтров
  (S0.2/F2/F7), «датаграмма отброшена», «соединение закрыто». Клиентская сторона того же кода
  (`pump` при `egress == None`) логирует как раньше — это устройство пользователя.
- **Issuer:** Layer-1-авторизация абонента, аренда single-session, исчерпание квоты, «подписано
  вслепую N токенов» (там же был IP пира), ошибки соединений (несли адрес в тексте), add/revoke по
  admin-каналу (сам реестр и есть audit-trail).
- Из текстов ошибок Layer-1 убран адрес пира — иначе он всплывал бы в любой строке логирования выше.
- Демо/e2e-стенды (`docker/entrypoint-{exit,exit-e2e,issuer}.sh`) ставят `Citadel_DEBUG_LOG=1`
  явно — тесты и демонстрация фильтров работают как раньше; прод-энтрипойнт от `AdminDeployer`
  переменную не задаёт.

**Оговорка.** Это no-logs *приложения*, не всей системы: у хостера/ядра остаются conntrack, netflow
и метаданные виртуалки. Речь о том, чтобы **сам сервис** не производил долгоживущего журнала
деанонимизации.

---

## 9. Стойкость мастер-пароля и «оракул ссылки» (2026-07-30)

### 9.1 Argon2id: с OWASP-минимума на ~1–2 с

Было `m=19 MiB, t=2, p=1` — рекомендованный минимум, но на современном CPU это ~45 мс на derive,
то есть дешёвый офлайн-перебор украденного `vault.bin` (а он несёт bearer-креды: `obfs_psk`,
seed'ы, пины). Разблокировка происходит раз за запуск, поэтому цена в секунду-две для человека
незаметна, а стоимость перебора растёт на два порядка.

| | m | t | p | ориентир времени |
|---|---|---|---|---|
| desktop (Linux/Windows/macOS) | 256 MiB | 4 | 1 | ~1 с на dev-машине, ~2 с на среднем ноуте |
| Android | 64 MiB | 8 | 1 | память дороже времени: OOM-killer и бюджетные устройства |

Параметры лежат В ФАЙЛЕ, поэтому старые хранилища читаются своими и **при первом же открытии
поднимаются** до текущих (`rekey`: новый salt + ключ + перезапись). Сравнение идёт по «стоимости»
`m×t`, чтобы файл, созданный сильнее (в т.ч. на другой платформе), не понижался. Не удалось
перезаписать — работаем на прочитанных параметрах: открыть хранилище важнее, чем усилить его.

### 9.2 Проверка `citadel://`-ссылки: что это и чем НЕ является

Разбор ссылки (`base64url` → CBOR → диапазон версии → валидация dns/routes) отвечал мгновенно и
неограниченно часто — вердикт выскакивал на каждый символ недописанной ссылки. Сделано: пауза после
ввода в UI, минимальное время ответа одинаково для валидной и битой ссылки, удорожание на серию
неудач подряд, сериализация проверок одним мьютексом; тот же ограничитель стоит на пути «добавить
профиль», иначе он остался бы быстрым способом проверять догадки.

**Границы применимости — важнее самой меры.** Разбор проверяет СТРУКТУРУ, а не секреты: любые 32
случайных байта в поле `obfs_psk` дают «валидна», поэтому подобрать через него ключи нельзя в
принципе. Вердикт полезен атакующему лишь для восстановления уже утёкшей, но повреждённой ссылки —
и такую атаку ведут не через наш UI: формат открытый, парсер публичный (репозиторий,
`citadel-linkgen`, сам бинарь клиента), догадки проверяются офлайн со скоростью миллионов в
секунду. Отсюда два вывода, которые нельзя терять: (1) удаление проверки из клиента ничего бы не
дало — оракул это сам формат; (2) настоящий рубеж против подбора кред **сетевой** (квоты
issuer/exit, single-session lease, отзыв), а против утечки ссылки — гигиена хранения (vault,
FLAG_SECURE, no-logs).
