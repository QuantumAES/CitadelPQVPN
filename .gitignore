# --- Rust ---
/target
**/*.rs.bk
*.pdb

# --- Артефакты релиза (tools/mk-release.sh) ---
/dist

# --- Python (.venv для tools/obfs_ref.py + cmake под aws-lc-rs) ---
.venv/
__pycache__/
*.py[cod]
*$py.class

# --- Docker: бинари копируются сюда run-demo.sh и пересобираются (не версионируем) ---
docker/citadel-m1
docker/citadel-token
docker/citadel-m0
docker/veil-*
docker/.env

# --- Сгенерированные секреты/материал (локальные копии вне Docker-тома) ---
*.pin
*.mldsa
*.psk
issuer.pub
issuer.key
/tokens
docker/tokens

# --- Релизный minisign-ключ подписи: СЕКРЕТ никогда не в git (коммитим только *.pub) ---
citadel-release.key
packaging/release/*.key
.citadel/

# --- ОС / редакторы ---
.DS_Store
*.swp
*~
.idea/
.vscode/

# --- Временные/мусорные ---
*.tmp
*.kate-swp
