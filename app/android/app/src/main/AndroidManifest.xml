<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- CitadelPQVPN (C3): VPN-туннель через foreground-сервис -->
    <!-- INTERNET — обязателен в release-манифесте: без него создание AF_INET-сокета падает
         с EACCES (os error 13), и коннект к exit невозможен. В debug/profile-манифестах его
         добавляет шаблон Flutter, но release собирается из main — там он ДОЛЖЕН быть явно. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- S2: registerNetworkCallback в CitadelVpnService требует ACCESS_NETWORK_STATE — без него
         callback молча падает (SecurityException в try/catch) и смена сети не форсит реконнект. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- #0.2: сканирование QR-ссылки камерой при добавлении профиля (mobile_scanner). Камера НЕ
         обязательна для работы VPN → required=false, чтобы не отсекать устройства без камеры. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="CitadelPQVPN"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- VpnService движка CitadelPQVPN (трек C3) -->
        <service
            android:name=".CitadelVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="systemExempted">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- C8.3 split-tunnel: видимость запускаемых приложений для picker'а (Android 11+ package
             visibility) — без чувствительного QUERY_ALL_PACKAGES. -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
