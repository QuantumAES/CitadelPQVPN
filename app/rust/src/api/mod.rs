pub mod citadel;
pub mod diag;
pub mod simple;
// Admin-режим (SSH-управление сервером). FFI-функции есть на всех платформах (frb ссылается на них
// безусловно), бэкенд десктоп/мобилка разведён внутри модуля (на мобилке — заглушка-отказ).
pub mod admin;
