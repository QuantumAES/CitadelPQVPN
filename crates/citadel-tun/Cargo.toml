[package]
name = "citadel-tun"
version = "0.1.0"
publish.workspace = true
edition.workspace = true
license.workspace = true
description = "CitadelPQVPN — TUN I/O (Linux /dev/net/tun via TUNSETIFF; Android — fd от VpnService)."

[target.'cfg(any(target_os = "linux", target_os = "android"))'.dependencies]
libc = "0.2"
