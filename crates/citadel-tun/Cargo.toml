[package]
name = "citadel-tun"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "CitadelPQVPN — TUN-устройство (Linux, /dev/net/tun via TUNSETIFF). Привилегированный путь data plane."

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
