[package]
name = "citadel-winnet"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CitadelPQVPN — платформо-нейтральное ядро Windows-модели W2 (IPC-кадры app↔служба, WFP-план, маршруты); общее для WindowsTunProvider и службы citadel-svc"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
ciborium = "0.2"
