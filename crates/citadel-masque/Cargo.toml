[package]
name = "citadel-masque"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "CitadelPQVPN — CONNECT-IP data plane: QUIC varint, HTTP-datagram payloads (RFC 9484/9297), capsules, IPv4/ICMP/UDP/DNS helpers"

[dependencies]

[dev-dependencies]
hex = "0.4"
criterion = { version = "0.5", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "parse"
harness = false
